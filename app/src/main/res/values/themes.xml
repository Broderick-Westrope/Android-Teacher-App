<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Whiteboard" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/mirage</item>
        <item name="colorPrimaryVariant">@color/mirage2</item>
        <item name="colorOnPrimary">@color/geyser</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/hoki</item>
        <item name="colorSecondaryVariant">@color/wild_blue_yonder</item>
        <item name="colorOnSecondary">@color/muave</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:itemBackground">@color/mirage</item>
        <item name="android:fontFamily">@font/monolisa_regular</item>
    </style>

    <!--  Custom Style for editing tasks in the to-do list  -->
    <style name="EditTaskStyle" parent="Theme.Design.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <!--  Custom Style for editing tasks in the to-do list  -->
    <style name="EditRecordStyle" parent="Theme.Design.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <!--  Used to override the action bar to avoid collisions with the navigation   -->
    <style name="Theme.Whiteboard.RootNoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!--  Used to override the action bar to avoid collisions with the navigation   -->
    <style name="Theme.Whiteboard.BranchNoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>